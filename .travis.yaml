language: generic

sudo: required
services:
  - docker

before_install:
  - docker build -t vbundalo/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true vbundalo/docker-react npm run test -- --coverage

